@model IEnumerable<Bike_store.Customer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.P_CustomerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.P_CustomerGender)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.P_CustomerMail)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.P_CustomerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.P_CustomerGender)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.P_CustomerMail)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CustomerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CustomerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CustomerId })
        </td>
    </tr>
}

</table>
